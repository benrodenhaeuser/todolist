# where should bundler look for rubygems?
source 'https://rubygems.org'

# what version of ruby do we want to use?
ruby '2.2.0'

# what are the gems we need?
gem 'minitest', '~> 5.10'
gem 'minitest-reporters', '~> 1.1'
gem 'stamp', '~> 0.4.0'
gem 'rake'

gemspec
